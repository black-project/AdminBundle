{% for key, flash in flashes %}
    {% for message in flash %}
    <div class="nNote {{ 'n' ~ key|capitalize }}">
        <p>{{ message|trans({}, "flash") }}</p>
    </div>
    {% endfor %}
{% endfor %}