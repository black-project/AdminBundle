<div id="sidebar">
    <div class="mainNav">
        <div class="user">
            <a title="" class="leftUserDrop">
                {% if app.security.getToken().getUser().getPerson() is defined %}
                {% if app.security.getToken().getUser().getPerson().getImage() is defined %}
                <img src="/admin/images/user.png" alt="" />
                {% else %}
                <img src="/admin/images/userLogin2.png" alt="" width="72" height="70" />
                {% endif %}
                {% else %}
                <img src="/admin/images/userLogin2.png" alt="" width="72" height="70" />
                {% endif %}
            </a>
            <span>{{ app.user.username }}</span>
            <ul class="leftUser">
                <li><a href="{{ url('admin_logout') }}" title="{% trans from "views" %}black.bundle.admin.admin.sidebar.logout.a.title{% endtrans %}" class="sLogout">{% trans from "views" %}black.bundle.admin.admin.sidebar.logout.a.text{% endtrans %}</a></li>
            </ul>
        </div>

        {#  Responsive nav  #}
        <div class="altNav">
            <div class="userSearch">
                <form action="">
                    <input type="text" placeholder="search..." name="userSearch" />
                    <input type="submit" value="" />
                </form>
            </div>

            {#  User nav  #}
            <ul class="userNav">
                <li><a href="{{ url('admin_logout') }}" title="{% trans from "views" %}black.bundle.admin.admin.sidebar.logout.a.title{% endtrans %}" class="logout">{% trans from "views" %}black.bundle.admin.admin.sidebar.logout.a.text{% endtrans %}</a>
            </ul>
        </div>

        {#  Main nav  #}
        <ul class="nav">
            {% include 'BlackAdminBundle:Admin:mainNav.html.twig' with {
                'a_route': 'admin_index',
                'a_text': 'black.bundle.admin.admin.sidebar.mainNav.dashboard.a.text',
                'a_title': 'black.bundle.admin.admin.sidebar.mainNav.dashboard.a.title',
                'img_alt': 'black.bundle.admin.admin.sidebar.mainNav.dashboard.img.alt',
                'img_path': 'dashboard.png'
            } only %}
        </ul>
    </div>
